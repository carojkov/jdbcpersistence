acquiring Connection from DriverManager and JNDI (2 ways should be sufficient for most usages)
optimistic locking - done. revisit the clob and blob part with regards to versioning
batch
views for updates (ID, NAME) (ID, LASTNAME, FIRSTNAME) etc... this should allow targeted updates.
handling null values ? next time write more
handling autogenerated columns will not be done because oracle and db2 do not support that
bring back support for *lob
